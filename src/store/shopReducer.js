import SHOP_DATA from '../data';


const INIT_STATE = {
    SHOP_DATA
};


const shopReducer = (state = INIT_STATE, action) => {
    switch(action.type){
        default: 
        return state;
    }
};


export default shopReducer;